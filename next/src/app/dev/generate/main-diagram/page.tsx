import { VideoPartGeneratePage } from "@/components/dev/VideoPartGeneratePage";

export default function DevMainDiagramGeneratePage() {
  return (
    <VideoPartGeneratePage
      part="main-diagram"
      title="Dev — Main Diagram Generator"
      description="Generate a deeper visual explanation as a direct LLM-authored canvas timeline. The model owns coordinates, layers, colors, timing, and animation."
    />
  );
}
