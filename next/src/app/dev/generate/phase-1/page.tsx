import { VideoPartGeneratePage } from "@/components/dev/VideoPartGeneratePage";

export default function DevPhaseOneGeneratePage() {
  return (
    <VideoPartGeneratePage
      part="phase-1"
      title="Dev — Phase 1 Generator"
      description="Generate and preview only the compact setup/context scene: its heading, supporting blocks, and setup graph."
    />
  );
}
